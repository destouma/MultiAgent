node_modules/
dist/
dist-electron/
release/
*.vsix
# Copied from the repo-root personas/ by vscode-extension/esbuild.js at build
# time; anchored so it doesn't match the real repo-root personas/ directory.
/vscode-extension/personas/
*.log
.DS_Store
Thumbs.db
.env
.env.*
*.db
*.db-*
*.tgz
# Maven build output (desktop-java/) - regenerated by mvn compile/test/javafx:run
desktop-java/target/
# Raw CodeQL extraction database - internal cache/index files that churn on every
# analysis run; not meant to be diffed or version-controlled. The actual scan
# deliverables (SARIF, SBOM) live alongside it and stay tracked.
release-verify/security-scan/codeql-db/

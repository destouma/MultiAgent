{
  "name": "multiagent-shared",
  "version": "0.0.0",
  "private": true,
  "description": "Root-level deps for shared/ so it resolves runtime packages (e.g. openai) without a full workspace setup. desktop/ and vscode-extension/ remain independently installable.",
  "dependencies": {
    "openai": "^5.8.2"
  }
}

{
  "name": "multiagent-shared",
  "version": "0.0.0",
  "private": true,
  "description": "Root-level deps for shared/ so it resolves runtime packages (e.g. openai) without a full workspace setup. vscode-extension/ (the only shared/ consumer) remains independently installable.",
  "dependencies": {
    "openai": "^5.8.2"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
